# Стан проєкту
Оновлено: 2026-09-11 15:30

| Пункт | Статус | Гейт | Коміт | Нотатка |
|-------|--------|------|-------|---------|
| 2.1   | DONE   | ✅ build/test | 27df1e2 | net8.0 явно вказано (SDK на машині — 10.0) |
| 2.2   | DONE   | ✅ build/test | 27df1e2 | EF Core 8.0.11 (пакети 10.x несумісні з net8.0), міграція InitialCreate створена |
| 2.3   | CODED  | ⏸ частково | 27df1e2 | `/health` написаний і перевіряє db+solver; повний прогін чекає на Docker Desktop (не запущений на машині) |
| 5.1   | DONE   | ✅ 5/5 тестів | a0552e1 | CP-SAT, релаксація sugar→kcal→budget+10%, 300 кандидатів <2с |
| 5.2   | DONE   | ✅ | a0552e1 | вбудовано в 5.1 |
| 8.1   | DONE   | ✅ 13/13 тестів | c38590b | 2 hex змінено відносно спеки 10.1 — див. технічний борг |
| 3.1   | DONE   | ✅ 14/14 тестів | 4f48b16 | Обрано офіційний SDK `ModelContextProtocol` 2.2.0 (не власний клієнт) — HttpClientTransport + кастомний HttpClient/DelegatingHandler покриває Streamable HTTP і auth. Gate: in-process stub-сервер (реальний SDK, не мок) підтверджує tools/list + запис у McpCalls |
| 3.2   | CODED  | ✅ 12 нових тестів | 4b49960 | OAuth 2.1+PKCE повний код. **`dotnet run -- auth` реально пройшов metadata+DCR проти живого mcp.silpo.ua і отримав справжній client_id** — залишається лише логін гостя (телефон+SMS, B-2), який не в моїй владі |
| 3.3   | DONE   | ✅ 3/3 тестів | 0daebe9 | per-tool TTL кеш; nutrient/price поки не розділені (треба живий payload) — техборг |
| —     | DONE   | ✅ 4/4 тестів | c5d5e7d | fix: backoff+jitter (429) і force-refresh (401) реально підключені в HTTP-пайплайн (раніше існували лише як окремий тестований клас, не викликались) |
| 3.4   | CODED  | ✅ 36/36 тестів | dffbe45 | coverage probe повний; попутно знайшов і виправив баг — CallToolAsync серіалізував SDK-обгортку замість тексту, тому весь парсинг завжди повертав порожньо. Живий прогін і реальний % покриття чекають на B-2 |
| 8.3   | DONE   | ✅ 1 тест (API) | 5721e3d | JS-підсвітка перевіряється вручну в браузері — не юніт-тестована |
| 4.0   | DONE   | ✅ 9/9 тестів | 9488981 | Обидві реалізації готові; вибір через `NutritionResolverFactory` — одна точка зміни, коли прийде рішення B-5 |
| 4.1   | CODED  | ✅ 1 тест | 3961ad1 | GuestContextCollector повний, схема полів припущена (як і 3.4) — потребує живого прогону |
| 4.2   | DONE   | ✅ 8/8 тестів | 3961ad1 | TargetNormsCalculator, 3 фікстури дають різні норми |
| 4.3   | DONE   | ✅ (модель) | 3961ad1 | BaselineCost — items/delivery окремо, без калькулятора (тривіальний конструктор) |
| 6.1   | CODED  | ✅ 2/2 тестів | f72c8cb | Пул кандидатів; розмір 200-400 потребує живого каталогу (B-2), логіка перевірена на фікстурі |
| 6.2   | DONE   | ✅ 1/1 тест | f72c8cb | 5 свопів з дельтами, коректно відхиляє гірший варіант |
| 7.1   | CODED  | ✅ 2/2 тестів | 0d6267f | Збірка кошика + перечитування, не довіряє success:true |
| 7.2   | DONE   | ✅ 1/1 тест | 0d6267f | **Ключовий пункт.** Тест навмисно підібраний так, щоб "очевидна" заміна в тій самій категорії НЕ була обрана — довів, що це справжній переролв, а не 1-в-1 підстановка |
| 7.3   | CODED  | ✅ 3/3 тестів | 0d6267f | Каскад 5 кроків у правильному порядку, підтверджено логом McpCalls; живий checkout — B-2/B-6 |
| 8.2   | DONE   | ✅ перевірено в браузері 375px | bba3b5e | Усі 6 екранів + empty/loading/error, реально відкрито через `dotnet run` + `/web/app`, скрін-шоти зроблені |
| —     | TODO   | | | Розділ 8 завершено. Наступний за TASKS.md: **розділ 9 (фініш)** — README, тег релізу; 9.1 (замикання циклу) можна пропустити за нестачею часу (10.3) |

## Потрібно від розробника
- **Найважливіше зараз:** `dotnet run --project src/MakroChef.Api -- auth` виводить реальне посилання на `https://mcp.silpo.ua/authorize` (DCR вже пройшла). Запустіть команду і встигніть залогінитись телефоном+SMS протягом 5 хв — це зніме блокер B-2 і дасть перший живий MCP-токен. Без цього розділи 3.4, 4, 6, 7 лишаться на рівні CODED (код є, живого прогону нема).
- Запустити Docker Desktop, щоб прогнати `docker compose up -d && ./scripts/gate.sh 2.3` наживо (перевірка `/health`).
- **Дизайн-рішення (не блокує, але варто підтвердити):** точні hex `--text-compare` (обидві теми) і `--text-muted` (темна тема) з розділу 10.1 не проходять WCAG AA 4.5:1 (виміряно: 3.0–3.84:1). Я підібрав ближчі відтінки тієї ж гами, що проходять гейт — деталі в коміті c38590b і в `web/tokens/tokens.css`. Якщо є фірмова палітра — скажіть, замінюю.
- Дивись `BLOCKERS.md` — GitHub remote, B-1 (знято фактично — DCR працює автоматично), B-2..B-6.

## Технічний борг
- `--text-compare` і темний `--text-muted` відрізняються від точних hex у TASKS.md 10.1 (змінено заради контрасту, задокументовано вище).
- Кеш `get_product_details` (3.3): нутрієнти й ціна кешуються разом на 15 хв замість "нутрієнти вічно, ціна 15 хв" — розділю, коли побачу реальну форму відповіді (B-2).
- `DbMcpAuthTokenProvider` написаний, але ще ніде не підключений через DI (чекає, поки з'явиться реальний userId/сесія в 4.x).
- Трейс-панель (8.3): візуальна підсвітка — чистий client-side JS/CSS, перевірено логікою (тест на дані), але не в реальному браузері. Відкрити `/web/trace/index.html?sessionId=<guid>` і подивитись очима, коли буде живий прогін.

## Рішення, зафіксовані по дорозі
- SDK на машині — .NET 10, усі проєкти явно `-f net8.0`, як вимагає TASKS.md 0.3.
- EF Core/Npgsql/Design закріплені на версії 8.0.11 (найновіші 10.x пакети не сумісні з net8.0).
- Python на машині не налаштований (Windows Store заглушка) — не критично, бо TASKS.md 0.3 тепер переносить солвер повністю на C#/Google.OrTools, окремого Python-сервісу більше немає.
