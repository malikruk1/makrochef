# Стан проєкту
Оновлено: 2026-09-14 (сесія живого прогону + Telegram знято + LLM-шар + трейс-панель UI)

| Пункт | Статус | Гейт | Коміт | Нотатка |
|-------|--------|------|-------|---------|
| 2.1   | DONE   | ✅ build/test | 27df1e2 | net8.0 явно вказано (SDK на машині — 10.0) |
| 2.2   | DONE   | ✅ build/test | 27df1e2 | EF Core 8.0.11 (пакети 10.x несумісні з net8.0), міграція InitialCreate створена |
| 2.3   | DONE   | ✅ `/health` живий | — | Docker Desktop полагоджено (4.20.1→4.34.3), Postgres реально піднятий і перевірений |
| 5.1   | DONE   | ✅ 5/5 тестів | a0552e1 | CP-SAT, релаксація sugar→kcal→budget+10%, 300 кандидатів <2с |
| 5.2   | DONE   | ✅ | a0552e1 | вбудовано в 5.1 |
| 8.1   | DONE   | ✅ 13/13 тестів | c38590b | 2 hex змінено відносно спеки 10.1 — див. технічний борг |
| 3.1   | DONE   | ✅ 14/14 тестів | 4f48b16 | Офіційний SDK `ModelContextProtocol` 2.2.0, in-process stub + реальний сервер обидва підтверджують tools/list |
| 3.2   | DONE   | ✅ жива OAuth-сесія | 4b49960 | OAuth 2.1+PKCE, DCR і токен реально отримані й збережені (AES-GCM) в Postgres проти живого `mcp.silpo.ua` |
| 3.3   | DONE   | ✅ живий прогін | 0daebe9 | `SessionBootstrap` (branchId/deliveryType/timeslot) підтверджено на живому кошику |
| —     | DONE   | ✅ 4/4 тестів | c5d5e7d | backoff+jitter (429) і force-refresh (401) підключені в HTTP-пайплайн |
| 3.4   | DONE   | ✅ живий прогін | 37f1e8e | `CoverageProbe` резолвить slug+нутрієнти по реальній історії чеків; стійкий до "поганих" SKU (BLOCKERS.md #14) |
| 8.3   | DONE   | ✅ 1 тест (API) | 5721e3d | Трейс-панель, JS-підсвітка перевіряється вручну |
| 4.0   | DONE   | ✅ живий прогін | 222cafb | Exact/CategoryIndex обирається автоматично через `CoverageProbe.CoveragePercent`; реальні ключі `product.attributes` звірено (B-5 знято) |
| 4.1   | DONE   | ✅ живий прогін | 58d13bd | `GuestContextCollector` — реальні профіль/родина/обмеження/адреси/лояльність, `/api/profile` |
| 4.2   | DONE   | ✅ 8/8 тестів | 3961ad1 | `TargetNormsCalculator`, 3 фікстури дають різні норми |
| 4.3   | DONE   | ✅ (модель) | 3961ad1 | BaselineCost — items/delivery окремо |
| 6.1   | DONE   | ✅ живий прогін: 173 кандидати | dcf6f15 | `CandidatePoolBuilder` + новий `CategoryResolver` (реальні slug-и категорій, не вигадані слова) |
| 6.2   | DONE   | ✅ 1/1 тест | f72c8cb | `SwapGenerator`, 5 свопів з дельтами, коректно відхиляє гірший варіант |
| 7.1   | DONE   | ✅ живий прогін | 180a6ed | `BasketAssembler` + `POST /api/basket/apply` (двофазне підтвердження очищення кошика), кнопка "До оформлення" підключена |
| 7.2   | DONE   | ✅ 1/1 тест + `POST /api/basket/reoptimize` | d71640c | Повний переролв при недоступності товару, не 1-в-1 підстановка |
| 7.3   | DONE   | ✅ живий прогін (B-6 знято) | 117f95b | Каскад 5 кроків; реальний живий чек показав: checkout-лінка в MCP не існує (жодного `checkout`/`place_order`/`pay`-інструменту) — `CheckoutLinks` тепер чесно несе `BlockingValidations` замість вигаданого посилання; бонус реально читається з кореня кошика, а не з `get_loyalty_info` |
| 8.2   | DONE   | ✅ усі 6 екранів на реальних даних | eb53121 | Усі екрани Mini App підключені до `/api/*`, перевірено в браузері |
| 9.1   | DONE   | ✅ живий прогін | 3012830 | `WeekOverWeekAnalyzer` (`GET /api/week-over-week`) — реальний дефіцит білка за 2 тижні з чеків, чесна апроксимація ваги (~100г/позиція) |
| 9.2   | DONE   | ✅ 83/83 у чистому клоні | 35c60e6 | README + тег `v1.0-hackathon`, GitHub push зроблено |
| **6** | **DONE** | ✅ **живий E2E**: `GET /api/basket` → `success:true` | 0dbd101 | **Головний цикл агента підтверджено на реальному акаунті**: чеки → дефіцит → кошик 1136.86 ₴ дешевший за звичний 1166.97 ₴, 1134 г білка проти цілі, без жодних послаблень |
| 8.2   | DONE   | ✅ рішення розробника, знято B-3 | 4c0cc02 | Telegram Mini App ніколи не був вимогою — веб-застосунок працює самостійно, реальна навігація додана, `.env.example`/TASKS.md оновлено |
| 0.2   | DONE   | ✅ 91/91 тестів, живий прогін | 701a638 | Пояснення свопів: `IAnthropicClient`/`AnthropicClient`/`SwapExplainer`, опційний `ANTHROPIC_API_KEY` з детермінованим фолбеком; `GET /api/basket/swaps` уперше реально підключає `SwapGenerator` до жодного ендпойнта до цього |
| 8.3   | DONE   | ✅ живий прогін: 16105 записаних викликів | 189030c | Екран 7 "Трейс" нарешті доданий у UI (бекенд існував, але не мав споживача) — темна тема, підсвітка повторного `solver.solve` і `out_of_stock`/`infeasible` |
| 9.2   | DONE   | ✅ живий прогін | 72bd31d | README переписано під поточний стан (без Telegram, 91 тест, реальні URL/порти, чесні обмеження без застарілих тверджень) |
| 0.2   | DONE   | ✅ 96/96 тестів, живий прогін | ba52524 | Переклад вільного тексту («без риби») в обмеження: `RestrictionTranslator` + таблиця `RestrictionOverrides` (додатково до MCP, MCP не має tool на видалення); третя й остання LLM-задача 0.2 закрита |

## Підсумок сесії 2026-09-14
Ця сесія — перехід від "код написаний, але не перевірений наживо" до **повністю підтвердженого живим прогоном
циклу**. Послідовно знайдено й виправлено 9 реальних, раніше невідомих багів (усі — тільки завдяки
реальному акаунту, жоден не був видимий на моках):

1. `silpo_*` tool-неймінг, вкладена структура кошика, `shoppingCartId`-параметр (уже було на момент
   попереднього чекпоінту).
2. `get_product_details` реально приймає `slug`+`branchId`, не `productId` — резолв slug доданий у весь
   пайплайн (`CandidatePoolBuilder`/`SwapGenerator`/`ReoptimizationService`/`CoverageProbe`).
3. `BasketAssembler`/`CheckoutCascade` викликали `get_shopping_cart_by_id` без `shoppingCartId` — той самий
   клас бага, що й у `CoverageProbe`, знайдений і виправлений тричі окремо.
4. MCP іноді повертає простий текст помилки замість JSON навіть при успішному виклику (знятий з продажу
   товар, ліміт розміру батчу) — `JsonFieldScanner.Parse` і кожен цикл резолву кандидата тепер це
   толерують, а не падають на всю партію.
5. Реальні ключі `product.attributes` звірено (`Білки`/`Жири`/`Вуглеводи` — правильні здогадки; цукор може
   бути відсутнім взагалі; енергія — рядок з КОМОЮ як десятковим роздільником, що без нормалізації
   мовчки спотворювало число в 10 разів).
6. `silpo_get_products`'s `category`-фільтр вимагає реальний slug з `silpo_get_categories`, а не вільний
   текст — новий `CategoryResolver`.
7. `EnumerateOrderLikeObjects` не розгортав обгортку `{"orders":[...]}`, офлайн-чек несе суму в `"sumReg"`,
   товари обох типів замовлень — у `"products"`, не `"items"` — бюджет солвера виходив нульовим.
8. Реальний `get_product_details` ніколи не повертає `"category"` — солверний ліміт "N одиниць на
   категорію" ставав глобальним лімітом на весь кошик; виправлено підписуванням кандидатів реальним
   ключовим словом пошуку + піднято дефолт ліміту з 3 до 5.
9. Checkout-лінка в MCP не існує взагалі (жодного `checkout`/`place_order`/`pay`-інструменту в
   `tools/list`); бонус реально лежить у кореневому `"loyalty"` кошика, не в `get_loyalty_info`.

Результат: **`GET /api/basket` на живому акаунті повертає `success:true`** з реальним оптимізованим
кошиком, дешевшим за звичний тиждень гостя. Усі 7 екранів застосунку (включно з трейс-панеллю)
підключені до реального бекенду. Тести — 96/96 (з 66 на початку сесії).

## Продовження сесії 2026-09-14 (після початкового чекпоінту)
Після переходу на "живий" стан і виявлення дев'яти вищезгаданих багів, сесія продовжилась ще на
~10 знахідок (BLOCKERS.md #24-32) і три функціональні доповнення:
- Знято B-3 рішенням розробника — Telegram Mini App ніколи не був вимогою, застосунок працює як
  самостійна веб-сторінка (реальна навігація додана замість Telegram-специфічного UI).
- Реалізовано LLM-шар (TASKS.md 0.2) повністю, з опційним `ANTHROPIC_API_KEY` і детермінованим
  фолбеком на кожен виклик: пояснення свопів, переклад вільного тексту в обмеження. Третя задача
  (нормалізація "брудних" назв з чека) визнана непотрібною — реальні назви Сільпо вже чисті.
- Додано екран 7 "Трейс" — бекенд (`GET /api/trace/{sessionId}`, `McpCalls`) уже існував, але без
  жодного UI-споживача; гейт 8.3 фізично не міг бути пройдений до цього.
- README переписано під поточний стан проєкту.

## Потрібно від розробника
- Нічого блокуючого не залишилось. **B-3 знято рішенням розробника** (Telegram — не вимога, застосунок
  іде як самостійна веб-сторінка).
- Опційно: значення `ANTHROPIC_API_KEY` у `.env`, якщо потрібні реальні (не шаблонні) LLM-пояснення
  свопів/переклад обмежень замість детермінованого фолбеку — без нього все так само повністю робоче.
- Дизайн-рішення з попередньої сесії (hex-контраст, розділ 10.1) — досі не підтверджено, не блокує.
- Дивись `BLOCKERS.md` — повний список із 32 пронумерованих живих знахідок та їхніх фіксів.

## Технічний борг
- Категорії резолвляться грубим підрядковим пошуком назви (`CategoryResolver`), а не повним деревом
  `silpo_get_categories_tree` — **досліджено 2026-09-14 (BLOCKERS.md #33) і свідомо залишено як є**:
  дерево несе тільки транслітеровані `slug`, без `title`, тож перехід вимагав би нового мапінгу
  кирилиця→транслітерація без явного виграшу для демо.
- `WeekOverWeekAnalyzer` апроксимує кожну позицію чека як ~100г (реальна вага з `displayRatio` для
  кожної історичної покупки означала б помножити кількість MCP-викликів на всю історію чеків).
- `MaxUnitsPerCategory=5` — емпіричний підбір під конкретний живий акаунт (173 кандидати, 6 категорій),
  не аналітично виведене значення; може знадобитись подальше тюнінгування на інших акаунтах/каталогах.
- `--text-compare` і темний `--text-muted` відрізняються від точних hex у TASKS.md 10.1 (змінено заради
  контрасту WCAG AA).
- `DbMcpAuthTokenProvider` написаний, але ще не підключений через DI (single dev-user поки що).

## Рішення, зафіксовані по дорозі
- SDK на машині — .NET 10, усі проєкти явно `-f net8.0`.
- EF Core/Npgsql/Design закріплені на версії 8.0.11.
- Python на машині не налаштований — не критично, солвер повністю на C#/Google.OrTools.
- Docker Desktop: стара 4.20.1 мала пошкоджений WSL2-образ, найновіша 4.90.0 вимагає новішу збірку
  Windows — вирішено проміжною версією 4.34.3.
