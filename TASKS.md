# TASKS.md — «Макро-шеф»: код

Єдиний файл для Claude Code. Тут **тільки те, що ти виконуєш сам**: пишеш код,
пишеш тести, ганяєш build/test локально, комітиш у локальний git.

Усе, що потребує людини (акаунт, ключі, OTP, GitHub, рішення) — у `BLOCKERS.md`.
Не намагайся це обійти й не зупиняй роботу через це: пиши код так, щоб він
запрацював, коли блокер знімуть, і рухайся далі.

Виконуй **строго зверху вниз**. Не починай наступний пункт, поки гейт
попереднього не зелений.

---

# 0. ПРО ПРОЄКТ

## 0.1. Що будуємо

Агент, який читає історію покупок гостя через MCP «Сільпо», рахує реальний
БЖВ-профіль і дефіцит по білку й цукру, збирає тижневий кошик, що цей дефіцит
закриває, переоптимізовує його при відсутності товару, застосовує знижки й
доводить до валідованого checkout. Кошик **не дорожчий** за звичний.

## 0.2. Розподіл відповідальності — ключове

- **LLM НЕ підбирає товари.** Тільки: нормалізує назви з чека, пояснює свопи,
  перекладає «без риби» у зміну обмежень.
- **Солвер підбирає товари.** MILP, детермінований, відтворюваний.
- **MCP — тільки I/O.** Жодної бізнес-логіки в шарі MCP-клієнта.

## 0.3. Схема

> **Рішення 2026-09-14**: Telegram Mini App ніколи не був вимогою MCP «Сільпо» чи
> хакатону — лише обраний UI-шар доставки. Розробник вирішив піти без Telegram
> (BLOCKERS.md B-3): UI — звичайна самостійна веб-сторінка (`web/app/`) з реальною
> навігацією між екранами, без Telegram Mini App SDK.

```
Веб-застосунок (web/app) ──► Backend (.NET 8) ──► MCP Silpo (HTTP, OAuth 2.1 + PKCE)
                            ├──► Postgres          (токени, кеш, McpCalls)
                            ├──► MakroChef.Solver   (Google.OrTools, CP-SAT)
                            └──► Anthropic API      (назви, пояснення)
```

**Усе на C#/.NET 8.** Солвер — не окремий сервіс, а проєкт у тому ж рішенні:
виклик у процесі, без HTTP і без другого контейнера. Мова в умовах хакатону не
регламентована — вимог рівно чотири, і жодна не стосується стека.

## 0.4. Заборонено в коді

- Вигадувати дані про товари, ціни, нутрієнти.
- Мокати MCP поза unit-тестами.
- Виводити цифру, походження якої не простежується до даних MCP.
- Хардкодити назви tools замість `tools/list`.
- Хардкодити hex-кольори в компонентах.
- Вигадувати вагу гостя — її немає в профілі «Сільпо».
- Екран ручного вводу їжі.
- Власний MCP-сервер, RAG, векторна база, розпізнавання чеків.

---

# 1. ПРОЦЕДУРА

## 1.1. Завершення пункту

```bash
./scripts/gate.sh <пункт>            # має вийти з кодом 0
git add -A
git diff --cached | grep -Ei "sk-ant|Bearer |password=|\+380|mcp_token" \
  && { echo "STOP: секрет у діффі"; exit 1; }
git commit -m "feat(mcp): OAuth 2.1 PKCE flow with encrypted token storage"
```

Коміти дрібні, Conventional Commits. Гілка на пункт: `step/3.2-oauth`.
Мердж у `main` локально через `git merge --squash`.
Коли з'явиться remote — `BLOCKERS.md` скаже, що робити далі.

## 1.2. Самоперевірка перед комітом

Читай власний дифф як рецензент, не як автор:

```bash
git diff --cached > /tmp/review.patch
```

Шукай: необроблені винятки на мережевих викликах · відсутній backoff · hardcode
назв tools · hardcode hex · запис у БД без транзакції · логування персональних
даних · магічні числа. Знайшов — виправляй у цьому ж коміті.

Чотири питання, відповідати чесно:
1. Чи є мок MCP поза тестами?
2. Чи потрапляє токен кудись, крім БД бекенду?
3. Чи я позначив пункт виконаним, знаючи, що частина не працює?
4. Чи додав я щось поза TASKS.md?

## 1.3. Файл стану `CHECKPOINTS.md`

Оновлювати після кожного коміту.

```markdown
# Стан проєкту
Оновлено: <дата час>

| Пункт | Статус | Гейт | Коміт | Нотатка |
|-------|--------|------|-------|---------|
| 2.1   | DONE   | ✅   | a1b2c3| |
| 3.4   | CODED  | ✅   | e4f5g6| потребує B-2 для живого прогону |

## Потрібно від розробника
## Технічний борг
```

Статуси: `TODO` · `DONE` (код написаний, гейт зелений) · `CODED` (код готовий,
живий прогін чекає на блокер) · `BLOCKED` (не можу писати код далі без рішення).

На початку сесії: прочитати `CHECKPOINTS.md`, сказати одним реченням, де ми,
продовжити з першого `TODO`. Не перечитувати весь код.

## 1.4. Робота з блокерами

Пункти, позначені 🔒, мають блокер у `BLOCKERS.md`. Правило:

- **Код пишеш повністю.** Разом із обробкою помилок і тестами на фікстурах.
- **Гейт — локальний.** Build, unit-тести, коректна поведінка без креденшелів
  (зрозуміла помилка, а не падіння зі стектрейсом).
- **Статус ставиш `CODED`**, дописуєш у «Потрібно від розробника», ідеш далі.
- **Не зупиняєшся.** Єдиний виняток — 🔴 у пункті 4.0: без рішення про фрейм
  наступний код писати немає сенсу.

---

# 2. ФУНДАМЕНТ

## 2.1. Каркас рішення

```bash
dotnet new sln -n MakroChef
dotnet new webapi   -o src/MakroChef.Api
dotnet new classlib -o src/MakroChef.Mcp
dotnet new classlib -o src/MakroChef.Domain
dotnet new classlib -o src/MakroChef.Nutrition
dotnet new classlib -o src/MakroChef.Solver
dotnet new classlib -o src/MakroChef.Agent
dotnet new classlib -o src/MakroChef.Data
dotnet new xunit    -o tests/MakroChef.Tests
dotnet sln add $(find src tests -name "*.csproj")
dotnet add src/MakroChef.Solver package Google.OrTools
mkdir -p scripts docs
git init -b main && printf '.env\nbin/\nobj/\n' > .gitignore
```

## 2.2. Інфраструктура

- `docker-compose.yml`: **тільки** postgres:16. Солвер у процесі, окремого
  контейнера немає.
- EF Core, перша міграція: `Users`, `McpTokens`, `ProductNutrition`,
  `ReceiptSnapshots`, `McpCalls`.
- `/health` — пінгує БД і перевіряє, що CP-SAT ініціалізується.
- `.env.example` із порожніми ключами: `ANTHROPIC_API_KEY`, `POSTGRES_PASSWORD`,
  `TOKEN_ENCRYPTION_KEY` (`TELEGRAM_BOT_TOKEN` прибрано 2026-09-14 — рішення піти
  без Telegram, див. BLOCKERS.md B-3 і TASKS.md 0.3/8.2).

## 2.3. Скрипт гейтів

`scripts/gate.sh` — створити до будь-якого іншого коду.

```bash
#!/usr/bin/env bash
set -euo pipefail
STEP="${1:?вкажи пункт}"
echo "=== GATE $STEP ==="
dotnet build --nologo -warnaserror
dotnet test --nologo
case "$STEP" in
  2.3) curl -fsS localhost:8080/health | grep -q '"db":"ok"' ;;
  5.1) dotnet test --nologo --filter "FullyQualifiedName~SolverTests" ;;
esac
echo "=== GATE $STEP PASSED ==="
```

**ГЕЙТ 2:** `docker compose up -d && ./scripts/gate.sh 2.3` → код 0,
`/health` = `{"db":"ok","solver":"ok","mcp":"unconfigured"}`.

---

# 3. MCP-КЛІЄНТ

## 3.1. Транспорт і логування викликів

- Streamable HTTP.

**Перша дія — перевірка SDK, не код.** У документації «Сільпо» приклади дані для
TypeScript, Vercel AI SDK і Python. C# серед них немає. Офіційний пакет
`ModelContextProtocol` існує, але клієнтська частина з OAuth 2.1 + Dynamic Client
Registration там опрацьована слабше за TS-ну.

Порядок:
1. Постав `ModelContextProtocol` і перевір, чи має він клієнт зі Streamable HTTP
   і підтримкою власного auth-провайдера. Таймбокс — 30 хвилин.
2. **Якщо має** — використовуй його.
3. **Якщо ні** — пиши власний тонкий клієнт на `HttpClient`. Streamable HTTP це
   звичайний HTTP із JSON-RPC 2.0 у тілі: `initialize` → `tools/list` →
   `tools/call`. Приблизно 200 рядків. Не воюй із SDK понад таймбокс.

У будь-якому разі інтерфейс `IMcpClient` у твоєму коді однаковий — решта проєкту
не має знати, що під ним.

Далі:
- На старті сесії — `tools/list`. Назви, аргументи, JSON Schema брати **звідти**.
- Кожен виклик логувати в `McpCalls`: tool, args_hash, статус, тривалість.
  Джерело даних для трейс-панелі (8.3).

**ГЕЙТ 3.1:** тест на локальному stub-сервері — клієнт коректно читає
`tools/list` і пише запис у `McpCalls`. У `CHECKPOINTS.md` зафіксувати, який шлях
обрано (SDK чи власний клієнт) і чому.

## 3.2. 🔒 OAuth 2.1 + PKCE `[B-1, B-2]`

Пишеш повністю:
```
401 → /.well-known/oauth-authorization-server → POST /register (DCR)
    → PKCE-пара → URL для /authorize → обмін коду на MCP-токен
    → Authorization: Bearer <token> у всіх запитах
```
Токен і refresh — у Postgres, AES-GCM під `TOKEN_ENCRYPTION_KEY`.
CLI-команда `auth` друкує URL і чекає на редірект.

Обробка помилок — закласти одразу:

| Код | Дія |
|---|---|
| `401 invalid_token` | refresh → повторний OAuth |
| `403` | м'яка деградація, логувати |
| `429` | експоненційний backoff **з jitter** (ліміти per-user через `Cookie: mcp-user={userId}`) |
| `-32601` | метод не підтримується |

**ГЕЙТ 3.2 (локальний):** unit-тести на генерацію PKCE, парсинг метаданих,
шифрування токена, backoff із jitter. `dotnet run -- auth` без креденшелів дає
зрозуміле повідомлення, а не стектрейс. Статус `CODED`.

## 3.3. 🔒 Сесія кошика і кеш `[B-2]`

Порядок на старті сесії:
```
silpo_get_my_shopping_cart → silpo_get_shopping_cart_by_id → silpo_get_time_slots
```
Тримати `branchId` / `deliveryType` / `timeslot` у сесії — без них не працюють
tools із позначкою 🔒 cart.

Якщо `exists: false` → `silpo_create_shopping_cart`, перед цим:
```
1. silpo_find_address → lat/lng/city/street/houseNumber/district
                        (houseNumber → параметр "house")
2. silpo_get_available_delivery_types(lat,lng) → deliveryType + branchId
   якщо branchId = null → silpo_list_branches(hasPickup=true)
3. silpo_get_time_slots(branchId, deliveryType) → timeslot
```
`companyId` не передається — зашитий у сервері.

Кеш: нутрієнти вічно, ціни 15 хв. Без кешу — 429 на першому ж прогоні.

**ГЕЙТ 3.3:** тест на stub — два послідовні `get_product_details` на той самий
товар дають рівно один мережевий виклик.

## 3.4. 🔒 Coverage probe `[B-2]`

Консольна команда `probe`:
```
get_my_offline_orders + get_my_online_orders
  → для кожного унікального SKU: get_product_details (кеш + backoff)
  → docs/coverage-report.md
```

Шаблон звіту (заповнюється при живому прогоні):
```markdown
Покриття: __%
Унікальних SKU: __
З повним Б/Ж/В/цукром: __
Розріз по категоріях: ...
Дірки: ваговий, власне виробництво, фреш
Медіанний тижневий чек: __ ₴
```

**ГЕЙТ 3.4:** команда відпрацьовує на stub-фікстурі з 20 товарів і генерує
коректний звіт. Статус `CODED` — живий прогін чекає на `B-2`.

---

# 4. ПРОФАЙЛЕР

## 4.0. 🔴 Розвилка — потребує рішення розробника `[B-5]`

Після живого прогону 3.4 стає відомий відсоток покриття нутрієнтів. Від нього
залежить логіка всього профайлера:

- **≥60%** → точний БЖВ із `get_product_details`.
- **<60%** → індекс кошика по категоріях + fallback Open Food Facts за
  штрихкодом (`GET https://world.openfoodfacts.org/api/v0/product/{barcode}.json`,
  брати сирі `nutriments`, **не** Nutri-Score — для українських товарів він часто
  не порахований; описовий User-Agent; кодити оборонно).

**Поки рішення немає:** реалізуй інтерфейс `INutritionResolver` із двома
імплементаціями і тестами на обидві. Вибір — через конфіг. Так жоден із
варіантів не блокує роботу, і після відповіді змінюється один рядок налаштувань.

## 4.1. 🔒 Збір контексту гостя `[B-2]`

| Tool | Навіщо |
|---|---|
| `get_my_offline_orders` | база БЖВ-профілю |
| `get_my_online_orders` | доповнення |
| `get_my_profile` | дата народження → вік |
| `get_my_family` | діти з віком → **норми на родину, не на одного** |
| `get_my_food_restrictions` | жорсткі виключення в солвері |
| `get_my_delivery_addresses` | є збережена → не викликаємо `find_address` |
| `get_loyalty_info` | баланс балабонусів |

## 4.2. Цільові норми

Три джерела за пріоритетом:
1. **Гість сказав сам** («хочу 120 г білка») — найвищий пріоритет.
2. **Порахували:** вік із профілю, склад родини; білок ≈ 1.2–1.6 г/кг;
   ВООЗ — вільні цукри <10% калорійності (ціль <5%); підсумувати на
   домогосподарство.
3. **Дефолт** для дорослого середньої активності, помітити як «оцінка».

Вагу не вигадувати: або питати одним питанням, або рахувати від калорійності.

## 4.3. BaselineCost

```
BaselineCost = медіанний тижневий чек + доставка за поточним тарифом
```
Порівнювати кошик із доставкою проти офлайн-чека без неї — брехня. Зберігати
роздільно: товари / доставка / разом.

**ГЕЙТ 4:** тести на трьох фікстурах чеків (одинак, родина з дитиною, з
обмеженнями) дають різні норми; сума спожито/норма/дефіцит сходиться; у виводі
є `%покриття` і джерело норми.

---

# 5. СОЛВЕР

Повністю автономний розділ — жодних блокерів. **Роби його першим**, паралельно
з розділом 3: він не залежить ні від MCP, ні від логіну.

## 5.1. Модель CP-SAT

`src/MakroChef.Solver`, пакет `Google.OrTools`, клас `BasketSolver` з методом
`Solve(SolverRequest) → SolverResult`. Виклик у процесі, без HTTP.

```
// x_i ∈ Z⁺ — кількість одиниць товару i
minimize   Σ price_i · x_i
s.t.
    Σ protein_i · x_i ≥ target_protein
    Σ sugar_i   · x_i ≤ max_sugar
    Σ kcal_i    · x_i ∈ [kcal_min, kcal_max]
    Σ price_i   · x_i ≤ baseline_cost        // ГОЛОВНЕ: не дорожче звичного
    Σ_{i∈cat}     x_i ≤ 3   ∀ cat            // культурна прийнятність
    x_i = 0  ∀ i ∈ restricted                // алергени/обмеження
    x_i ≤ 4                                  // без 12 пачок сиру
```

Реалізація через `CpModel` / `CpSolver`:
- `model.NewIntVar(0, 4, $"x{i}")` — змінна на товар.
- `LinearExpr.WeightedSum(vars, coeffs)` для кожного обмеження.
- `model.Minimize(...)` — цільова функція.
- `solver.StringParameters = "max_time_in_seconds:5"` — жорсткий таймаут, щоб на
  демо не було зависання.

**CP-SAT працює з цілими числами.** Ціни й нутрієнти подавай у сотих
(копійки, міліграми) і ділі на виході, інакше округлення з'їсть точність.

Ціни подавати **після знижок** (акція, купон, врахований баланс балабонусів).

## 5.2. Інфізибельність

Не падати. При `CpSolverStatus.Infeasible` послаблювати у фіксованому порядку,
щоразу перезапускаючи модель:
```
1) стеля цукру → 2) коридор калорій → 3) бюджет +10%
```
Повертати `Relaxed: [...]` у результаті. Чесність тут = бали за інженерію.

**ГЕЙТ 5:** три тести (нормальний, інфізибельний, з обмеженнями); розв'язання
< 2 с на 300 синтетичних кандидатах. Перевіряти через
`dotnet test --filter "FullyQualifiedName~SolverTests"`.

---

# 6. ПІДБІР КАНДИДАТІВ

## 6.1. 🔒 Набір пулу `[B-2]`

```
1. get_categories_tree     → мапа товар→категорія
                             (без неї обмеження «≤3 на категорію» неможливе)
2. find_products_batch     → до 30 товарів паралельно
3. get_products(категорія, акція) → добір у дефіцитних категоріях
                             (сир, риба, яйця, бобові), з пагінацією
4. get_promotions          → акції в цьому магазині
5. get_my_coupons          → список
   get_coupon_details(кожен) → УМОВИ й товари купона
                             (без цього купон можна тільки вгадати)
   get_my_promos           → персональні пропозиції
6. get_my_favorites        → бонус до «прийнятності»
7. нутрієнти з кешу → JSON у солвер
```

**Пул — 200–400 товарів.** Менше — бідне рішення солвера. Більше — rate-limit.

## 6.2. Свопи 1-в-1

Для кожного товару зі звичного кошика — альтернатива **в тій самій категорії**
через `get_similar_products`. Формат виграшу:

> йогурт X → йогурт Y: **+9 г білка, −11 г цукру, −3 ₴**, зараз по акції

Не «купіть кіноа». Переключення в межах категорії.

**ГЕЙТ 6:** на stub-фікстурі з 300 товарів формується коректний JSON для
солвера; генератор свопів дає ≥5 пар із порахованою дельтою по Б/цукру/ціні.

---

# 7. ЗБІРКА КОШИКА

## 7.1. 🔒 Базовий шлях `[B-2]`

```
clear_shopping_cart (питати гостя, якщо кошик не порожній)
  → add_or_update_cart_products (productId + companyId + branchId)
  → get_shopping_cart_by_id → читати validations[]
```
Не довіряти `success: true` — перечитувати кошик.

**ГЕЙТ 7.1:** тест на stub — кошик складається, `validations[]` читається,
сума ≤ baseline.

## 7.2. Переоптимізація замін — КЛЮЧОВИЙ ПУНКТ

```
ЯКЩО є проблемна позиція:
    get_replacements(problemProductId)
    → зібрати нутрієнти й ціни замін
    → ВИКЛИКАТИ СОЛВЕР ЗАНОВО:
          • виключити недоступний товар (x_i = 0)
          • додати заміни в пул кандидатів
          • ті самі обмеження
    → новий оптимальний кошик ЦІЛКОМ
    → remove_cart_products (позиції, що випали)
    → add_or_update_cart_products (нові)
    → перевалідувати
    → ліміт 3 ітерації, далі деградуємо і кажемо чесно
```

**Чому не проста підстановка.** Якщо недоступний йогурт із 40 г білка замінити
на йогурт із 22 г, бюджет звільнився, дефіцит виріс, а решта кошика лишилась
старою. Правильно — перерахувати цілком: можливо, тепер вигідніше взяти інший
сир і на пачку менше горіхів.

Це різниця між «агент підставив заміну» і «агент перепланував кошик».

**ГЕЙТ 7.2:** тест зі штучно недоступним товаром — новий кошик відрізняється
більш ніж однією позицією, бюджет утримано, дефіцит не виріс. Обов'язково
передбач прапорець `--simulate-out-of-stock=<productId>` для демо.

## 7.3. 🔒 Каскад здешевлення і checkout `[B-2, B-6]`

Порядок обов'язковий:
```
а) get_my_premium_subscription → Premium → інші умови доставки → інший baseline
б) get_my_certificates → add_or_update_certificates
в) get_promo_codes → update_shopping_cart(promoCode)  (найвигідніший)
г) ЯКЩО bonusAvailable > 0 І bonusRequested == null І isEnabled:
       спитати «У вас є {bonusAvailable} балабонусів. Застосувати?»
       → update_shopping_cart(bonusRequested = bonusAvailable)
д) get_shopping_cart_by_id → checkoutWebLink + checkoutMobileLink
```

**ГЕЙТ 7.3:** тест на stub проходить усі п'ять кроків у правильному порядку;
у логу видно кожен. Статус `CODED` — живий checkout перевіряє розробник.

---

# 8. ІНТЕРФЕЙС

## 8.1. Токени дизайну

Палітра в CSS-змінні. Жодного хардкоду hex у компонентах.

```css
:root {
  --bg:#FBF7F0; --card:#FFF; --border-card:#F0E9DC; --border-screen:#EAE3D6;
  --text:#2A2622; --text-2:#7A756C; --text-muted:#6E675C; --text-compare:#8A8172;
  --accent:#FF6B1A; --accent-soft:#F2A03D; --accent-text:#C4551A;
  --accent-plate:#FBEBD6; --on-plate:#85662E; --success:#3B6D11;
  --r-card:12px; --r-btn:12px; --r-pill:20px; --h-btn:46px;
}
```

**ГЕЙТ 8.1:** `grep -rE "#[0-9A-Fa-f]{6}" src/**/components` порожній, окрім
файлу токенів; контраст `--text-muted` і `--text-compare` на `--bg` і `--card`
≥ 4.5:1 (додай тест-скрипт розрахунку контрасту).

## 8.2. Веб-застосунок (раніше — Telegram Mini App, знято `[B-3]` 2026-09-14)

Самостійна веб-сторінка (`web/app/`), не Telegram-обгортка — Telegram ніколи не
був вимогою MCP чи хакатону, лише обраний і згодом відкинутий UI-шар доставки.
Реальна навігація між екранами (кнопки "Підключити Сільпо" → профіль → кошик →
checkout → тиждень-до-тижня) реалізована в самому застосунку, замість того, щоб
покладатись на нативне перемикання екранів Telegram.

| # | Екран | Ключові елементи |
|---|---|---|
| 1 | Підключення | Цінність одним реченням, кнопка, прогрес читання чеків |
| 2 | Профіль | Метричні картки БЖВ, смужка «звідки береться», покриття чека |
| 3 | Кошик | Порівняння двох кошиків, свопи, склад ціни, CTA |
| 4 | Переоптимізація | Банер + повторний прогін солвера |
| 5 | Checkout | Підтвердження + «наступного тижня перевіримо дефіцит» |
| 6 | Тиждень-до-тижня | Якщо лишився час (9.1) |

Плюс три стани для кожного екрана з даними: **порожньо**, **завантаження**
(скелетони, не спінер), **помилка MCP** (що сталось + повтор). Стан завантаження
знадобиться на демо — прогін не миттєвий.

**ГЕЙТ 8.2:** усі 6 екранів рендеряться, при 360 px немає горизонтального
скролу; тап-таргети ≥ 44 px. Статус `DONE` — усі екрани підключені до реальних
даних і мають реальну навігацію (CHECKPOINTS.md).

## 8.3. Трейс-панель

`/trace/{sessionId}` — дані з `McpCalls` у реальному часі: tool, аргументи, час,
статус. **Єдиний екран у темній темі**, моноширинний лог.

Два рядки виділити окремим кольором — на них тримається пітч:
`validations: 1 out of stock` і повторний `solver.solve()`.

**ГЕЙТ 8.3:** на записаній сесії з БД панель показує ≥20 викликів у порядку
виконання; рядок повторного солвера візуально виділений.

Статус `DONE` (2026-09-14) — екран 7 "Трейс" підключений до `GET /api/trace/{sessionId}`,
темна тема (тільки цей екран), моноширинний лог; повторний `solver.solve` і будь-який
`out_of_stock`/`infeasible` рядок виділені лівим бордюром і плашкою. Підтверджено живим
прогоном: реальна сесія показала 16105 накопичених викликів (лог обрізається до останніх
300 для продуктивності, з поясненням над логом).

---

# 9. ФІНІШ

## 9.1. Замикання циклу (якщо є час)

Порівняння gap тиждень-до-тижня. Валідація виміряною дельтою. Якщо
використовуєш історичні дані — **позначити явно**, що це ретроспектива.

## 9.2. Реліз

`README.md`: проблема, архітектура, запуск, таблиця покриття tools (26/40),
чесні обмеження. Тег `v1.0-hackathon`.

**ГЕЙТ 9:** `git clone` у чисту папку + `docker compose up` + README доводять до
працюючого застосунку без додаткових знань.

---

# 10. ДОВІДНИК

## 10.1. Стиль

**Три принципи.** Кремова база, помаранчевий тільки на акцентах. Щільно, але
біля кожного числа є друге число для порівняння. Виглядає як таб усередині
«Сільпо».

**Помаранчевий має рівно три ролі:** статус відхилення від норми, головна дія,
фінальна цифра «до сплати». Четвертої не існує. Зелений — тільки де є реальний
виграш.

### Світла тема

| Роль | Hex |
|---|---|
| Тло / картка | `#FBF7F0` / `#FFFFFF` |
| Межа картки / екрана | `#F0E9DC` / `#EAE3D6` |
| Текст основний / вторинний / приглушений | `#2A2622` / `#7A756C` / `#6E675C` |
| Число-порівняння | `#8A8172` |
| Акцент / приглушений / текст | `#FF6B1A` / `#F2A03D` / `#C4551A` |
| Плашка / текст на плашці | `#FBEBD6` / `#85662E` |
| Успіх | `#3B6D11` |

### Темна тема (тільки трейс-панель)

| Роль | Hex |
|---|---|
| Тло / картка / межа | `#16130E` / `#201B14` / `#2E2820` |
| Текст основний / вторинний / приглушений | `#F2EDE4` / `#A79E90` / `#8A8172` |
| Неактивний / роздільники | `#6E665A` / `#5A5346` |
| Акцент / акцент-текст | `#FF6B1A` / `#FF8A4C` |
| Плашка / текст на плашці | `#3A2A15` / `#FFC98A` |
| Текст на CTA / успіх | `#16130E` / `#A4D96B` |

### Типографіка

Один шрифт без засічок. **Дві товщини: 400 і 500.** Ніяких 600/700.

| Елемент | Розмір | Товщина |
|---|---|---|
| Головне число («до сплати») | 32 px | 500 |
| Ціни в порівнянні | 19 px | 500 |
| Числа в картках БЖВ | 20 px | 500 |
| Назва застосунку, CTA | 15 px | 500 |
| Тіло, значення | 12–13 px | 400/500 |
| Підписи, дельти | 11 px | 400 |
| Секційний заголовок | 11 px, `letter-spacing:.04em`, UPPERCASE, приглушений |

**Головний прийом — парні числа.** Ніколи «62 г». Завжди «62 / 105 г», рядком
нижче «бракує 43 г».

### Форма й розміри

Радіуси: картки й кнопки 12 px, рамка телефона 26 px, пігулки 20 px.
Односторонній акцент (лівий бордюр 3 px) → радіус цього кута **0**.
Відступи: секції 16 px, усередині картки 10–12 px, ритм 12–14 px.
Ширина 340–380 px. Не більше двох колонок. Кнопки 46 px, тап-таргети ≥ 44 px.

### Компоненти

- **Метрична картка** — білий фон, лівий бордюр 3 px, підпис → парне число →
  дельта зі стрілкою.
- **Смужка «звідки береться»** — стек-бар із 4 сегментів + легенда + рядок
  пояснення. Пояснює логіку агента без графіків.
- **Картка порівняння** — звичний → стрілка → оптимізований; нижче «було →
  стало (дельта %)».
- **Банер переоптимізації** — плашка з лівим бордюром 3 px. Тільки після
  реального перерахунку.
- **Рядок свопу** — стара назва закреслена → стрілка → нова напівжирна; нижче
  виграш. Зелений при чистій вигоді, приглушений при компромісі.
- **Склад ціни** — знижки зеленим із мінусом → розділювач → «До сплати» великим
  помаранчевим.
- **Нижня панель** — прилипла донизу, вторинна кнопка вузька зліва, CTA на решту.

### Заборонено в UI

Екран ручного додавання їжі · помаранчевий як фон великих площ · понад дві
товщини шрифту · понад дві колонки на мобільному · вигадана вага гостя ·
hardcode hex у компонентах.

## 10.2. Порядок робіт без блокерів

Якщо `B-2` (OAuth) ще не знято — це порядок, у якому код пишеться без простою:

```
2 (фундамент) → 5 (солвер на CP-SAT, повністю автономний) → 8.1 (токени)
→ 3 (MCP-клієнт на stub) → 8.3 (трейс-панель на записаних даних)
→ 4.0 (обидві імплементації резолвера) → 6, 7 на stub-фікстурах
→ 8.2 (Mini App на моках)
```

Реально блокує лише живий прогін і фінальна валідація. Код пишеться весь.

## 10.3. Порядок деградації

Не встигаєш — ріж **у цьому порядку**:
1. Замикання циклу (9.1)
2. Свопи: з ≥5 до топ-3
3. Каскад здешевлення: лишити тільки балабонуси
4. Трейс-панель → читабельний JSON-лог у файл

**Ніколи не ріж:** переоптимізацію замін (7.2), чесність цифр, серверне
зберігання токенів.

## 10.4. Покриття tools: 26 із 40

| Група | Використовуємо | Пропускаємо свідомо |
|---|---|---|
| Локація (6) | `find_address`, `get_available_delivery_types`, `list_branches`, `get_time_slots` | Нова Пошта (2) |
| Пошук (7) | `find_products_batch`, `get_products`, `get_product_details`, `get_similar_products`, `get_replacements`, `get_my_favorites` | `add_or_update_favorite_products` |
| Каталог (6) | `get_promotions`, `get_categories_tree`, `get_category` | `get_popular_categories`, `get_categories`, `get_product_sets` |
| Кошик (8) | всі 8 | — |
| Замовлення (2) | обидва | — |
| Профіль (4) | всі 4 | — |
| Лояльність (7) | всі 7 | — |

---

# 11. ПРОМПТИ

**Перша сесія:**
> Прочитай TASKS.md і BLOCKERS.md. Створи CHECKPOINTS.md. Виконай розділ 2 за
> процедурою 1.1. Далі зупинись і покажи стан.

**Наступні:**
> Прочитай CHECKPOINTS.md. Скажи одним реченням, де ми. Виконай наступний пункт
> зі статусом TODO за процедурою 1.1. Блокери не обходь — став CODED і йди далі.

**Якщо застряг:**
> Зупинись. Не пиши код. Опиши: що не працює, що пробував, які варіанти з
> оцінкою часу, який рекомендуєш і чому.
